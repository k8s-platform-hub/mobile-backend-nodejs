- args:
    permission:
      columns:
      - gender
      - name
      filter:
        user_id:
          $eq: X-HASURA-USER-ID
    role: user
    table: user_details
  type: create_update_permission

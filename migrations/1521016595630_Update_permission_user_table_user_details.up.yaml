- args:
    permission:
      columns:
      - user_id
      - name
      - gender
      filter:
        user_id:
          $eq: X-HASURA-USER-ID
    role: user
    table: user_details
  type: create_select_permission

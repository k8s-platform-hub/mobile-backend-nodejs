- args:
    permission:
      columns:
      - user_id
      - institution_name
      - degree
      - id
      filter:
        user_id:
          $eq: X-HASURA-USER-ID
    role: user
    table: user_education
  type: create_select_permission
